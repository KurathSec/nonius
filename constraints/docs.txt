# Docs toolchain. Kept SEPARATE from ci.txt on purpose: the docs stack must never be
# able to perturb measurement CI.
#
# No extras markers here. pip refuses a constraints file containing an extra
# ("ERROR: Constraints cannot have extras"), which silently makes the whole install
# fail; the extra belongs on the requirement rather than on the constraint.
mkdocs==1.6.1
mkdocs-material==9.5.49
